package product;

public class Meat extends Product {

public Meat(String Name, double calorie, double protein) {
        setCategory("Мясо");
        setName(Name);
        setCalorie(calorie);
        setProtein(protein);
}
}